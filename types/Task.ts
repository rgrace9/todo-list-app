// types.ts (optional: if you want to separate your types into another file)
export interface Task {
  id: string; // Use a string or number depending on your use case
  title: string;
  description: string;
  completed: boolean;
}
